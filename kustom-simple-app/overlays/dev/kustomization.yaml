bases:
- ../../base 

namespace: dev

patches:
  - path: replicas.yaml
    # target:
    #   kind: Deployment
    #   name: k8s-study-app

configMapGenerator:
- name: mykustom-map
  env: config.properties