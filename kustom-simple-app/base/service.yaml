apiVersion: v1
kind: Service
metadata:
  name: k8s-study-app-service
spec:
  type: LoadBalancer
  selector:
    app: k8s-study-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80   # must match containerPort
